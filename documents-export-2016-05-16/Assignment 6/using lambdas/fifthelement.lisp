(print ((lambda (val) (car (cdr (cdr (cdr (cdr val)))))) (read))
)
'the lambda function is a single line function which gets executed to do a particular nd simple job.
'the syntax of lambda function is quite simple and it is:-
'((lambda (parameters) operation) arguments)
'the body of lambda is simple (lambda (param) operate)
'Example :- ((lambda (a b) (+ a b)) 3 4) will return 7.
